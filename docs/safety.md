# Safety

The v1 application should be read-only.

Fault clearing, actuator tests, or write commands must remain disabled until the protocol is understood and the UI can require explicit user confirmation with clear warnings.

Hardware designs should protect both the vehicle electronics and the connected computer.
